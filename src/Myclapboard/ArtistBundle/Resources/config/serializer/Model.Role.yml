Myclapboard\ArtistBundle\Model\Role:
    exclusion_policy: ALL
    relations:
        -
            rel: movie
            href: 
                route: myclapboard_movie_get_movie
                parameters:
                    id: expr(object.getMovie().getId())
                absolute: true
            exclusion:
                groups: [cast, artist]
        -
            rel: artist
            href: 
                route: myclapboard_artist_get_artist
                parameters:
                    id: expr(object.getArtist().getId())
                absolute: true
            exclusion:
                groups: [awardList]
    properties:
        artist:
            expose: true
            groups: [movieList, movie]
        movie:
            expose: true
            groups: [artist, role]
        awards:
            expose: true
            groups: [artist]
